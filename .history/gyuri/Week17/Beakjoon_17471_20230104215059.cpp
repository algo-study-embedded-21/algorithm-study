#include <iostream>
#include <vector>
#define endl '\n'
using namespace std;

int N;
vector<int> regions;
int graph[15][15];

void Input() {

    cin >> N;

    for ( int i = 0; i < N; i++ ) {
        int region;
        cin >> region;
        regions.push_back(region);
    }

    for ( int i = 0; i < regions.size(); i++ ) {
        graph[]
    }
}

void Output() {}

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    return 0;
}
